#include <stdio.h>

int soma (int);

int main (){
    int n;
    printf ("digite o valor: ");
    scanf ("%d", &n);
    printf ("Soma dos elementos de 1 a %d: %d", n, soma (n));
}

int soma (int n){
    if (n==1){
        return n;
    }
    return soma(n-1) + n;
}
