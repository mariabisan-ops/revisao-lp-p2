#include <stdio.h>

int fatorial (int);

int main(){
  int n;
  printf ("N: ");
  scanf ("%d", &n);
  printf ("\nFatorial de %d: %d", n, fatorial(n));
}

int fatorial (int n){
  if (n==0 || n==1){
    return 1;
  }
  return n*fatorial(n-1);
}
