#include <stdio.h>

int fibonacci (int);

int main(){
  int n;
  printf ("N: ");
  scanf ("%d", &n);
  printf ("%d-esimo termo da sequencia: %d", n, fibonacci (n));
}

int fibonacci (int n){
  if (n==1){
    return 0;
  }
  if (n==2){
    return 1;
  }
  return fibonacci (n-1) + fibonacci (n-2);
}
